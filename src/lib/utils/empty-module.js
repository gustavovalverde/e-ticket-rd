/**
 * This file serves as an empty module stub.
 * It is used in `next.config.ts` with Turbopack's `resolveAlias`
 * to prevent certain modules from being bundled, effectively replacing
 * the `alias: false` pattern from webpack.
 */
const emptyModule = {};

export default emptyModule;
